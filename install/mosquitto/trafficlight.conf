# Traffic-light Ethernet is an isolated local network (10.77.0.0/24).
# Explicit listener is required on Mosquitto 2.x; otherwise a no-config broker
# commonly listens only on localhost.
listener 1883 10.77.0.1
allow_anonymous true
persistence true
persistence_location /var/lib/mosquitto/
